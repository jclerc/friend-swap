<div class="alerts">
  <% flash.each do |type, message| %>
    <div class="alert <%= bootstrap_class_for(type) %> fade show alert-dismissible">
      <button class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
  <% end %>
</div>
